# Contributing

Before submitting changes:

```bash
npm install
npm run typecheck
npm run build
```

Keep the application local-first. Do not add hardcoded credentials, analytics/tracking by default, or automatic upload of script contents. New cloud integrations must be explicit opt-in and documented.
